{
  "manifest_version": 3,
  "name": "Copy Text Fragment Link",
  "version": "1.0",
  "description": "Copy a text fragment link for the selected text via the context menu.",
  "permissions": ["menus"],
  "background": {
    "scripts": ["background.js"],
    "service_worker": "background.js"
  }
}
